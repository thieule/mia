# mia-ai (`mia`)

Lightweight AI assistant framework (Python package). This directory is the **editable install root** for `pip install -e ./core[...]` from sibling deployments (e.g. `ai-tech`).

See repository layout and deployment docs in the parent workspace as applicable.
