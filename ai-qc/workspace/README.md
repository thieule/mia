# Mia QC workspace (repo seed)

**Mia QC** tập trung **kiểm thử API**, **E2E / UI**, và tài liệu test. Cấu hình workspace: `./workspace` trong `config/config.json`. `memory/`, `cron/`, `sessions/` runtime — `../.gitignore`.

| Path | Role |
|------|------|
| **`AGENTS.md`** | Vai trò QC, API, UI |
| **`docs/AI_PROJECT_WORKSPACE_SPEC.md`** | Đồng bộ layout project nếu cần |
| **`TOOLS.md`** | Pytest MCP, `exec`, E2E |
| **`SOUL.md`**, **`USER.md`** | Giọng, base URL / môi trường test |
| **`admin/`** | Chính sách |
| **`agent/`** | Bản ghi chạy test, báo cáo lỗi nháp, `test-runs/` (JSON) |
