# Long-term Memory

This file stores important information that should persist across sessions.

## User Information

(Important facts about the user)

## Preferences

(User preferences learned over time)

## Project Context

(Information about ongoing projects — e.g. under `## Project: <slug>` list `github_repository`, `documents_storage_path`, `WORK_ROOT`, and values from **Agile Studio MCP** (`agile_project_get` / `agile_projects_list`) or the user when not in prompts.)

Optional: note **story keys** (`{slug}-{number}`), **story task** IDs you created, or **release** names if relevant to continuity. **Stories ≠ story tasks** (tasks are checklist rows on a story) — infer exact fields from your **Agile Studio MCP tools** when wired.

## Important Notes

(Things to remember)

---

*This file is automatically updated by mia when important information should be remembered.*
